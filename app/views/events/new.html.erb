<h1><%= @event.tournament.organization %> Registration</h1>

<%= form_with model: [@event.tournament, @event] do |f| %>
    <%= f.label :register_now? %>
    <%= f.hidden_field :tournament_id %>
    <%= f.check_box :registration_status %>
    <%= f.submit "Oss" %>
<% end %>